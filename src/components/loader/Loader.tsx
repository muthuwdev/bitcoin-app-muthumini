import React from 'react';
import { LoadingPanel } from '../../styles/Loader.styles';

const Loader = () => {
  return (
    <LoadingPanel>
      <div></div>
    </LoadingPanel>
  );
};

export default Loader;
